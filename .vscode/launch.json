{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Executar C (gdb)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/Game.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true, // Abre um terminal separado para mostrar o printf
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "preLaunchTask": "build"
    }
  ]
}
